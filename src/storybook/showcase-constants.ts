/** Shared Storybook playground inset (Atoms / Components). */
export const SHOWCASE_PLAYGROUND_PADDING_PX = 128;

/** Default white board padding when Figma inset is unspecified. */
export const SHOWCASE_BOARD_PADDING_PX = 64;
