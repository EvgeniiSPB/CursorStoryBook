/** Figma `card / image` frame (4208:704). */
export const CARD_IMAGE_FIGMA_NODE_ID = '4208:704';

/** Fixed width of every `cardImage - *` block. */
export const CARD_IMAGE_WIDTH_PX = 496;

/** Vertical gap between blocks on the Figma board (`gap-[64px]`). */
export const CARD_IMAGE_BOARD_GAP_PX = 64;

/** Board padding on the Figma frame (`p-[128px]`). */
export const CARD_IMAGE_BOARD_PADDING_PX = 128;

export const CARD_IMAGE_FIGMA_NODE_IDS = {
  '1:1': '4210:985',
  '4:3': '4210:986',
  '4:5': '4217:1227',
} as const;
