export function App() {
  return (
    <main style={{ padding: 'var(--spaces-static-400, 16px)' }}>
      <h1 style={{ fontSize: 'var(--font-sizes-headline-m)', margin: 0 }}>
        Design System
      </h1>
      <p style={{ color: 'var(--text-secondary, var(--primary-primary))' }}>
        Токены из Figma подключены. Откройте Storybook для компонентов.
      </p>
    </main>
  );
}
